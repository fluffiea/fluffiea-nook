# lint-staged

`lint-staged` 只针对暂存区的修改运行检查或者格式化任务的工具，这样的话就不用对整个项目进行检查或者格式化，大大提升效率。

需要搭配 `husky` 中的钩子来执行

## 安装

::: tabs

== npm

```bash
npm install --save-dev lint-staged
```

== pnpm

```bash
pnpm add --save-dev lint-staged
```

:::
